{
  "framework": "vite",
  "buildCommand": "npm run --workspaces=false build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!api/)[^.]+)", "destination": "/" },
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}